import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  headerContainer: {
    marginTop: 32,
    paddingHorizontal: 24,
    display: 'flex',
    alignItems: 'center',
  },
  headerTitle: {
    fontSize: 24,
    fontWeight: '600',
  },
  searchContainer: {
    margin: 16,
  },
});
